# {{ team_name }} Team - Claude Code Instructions

> {{ description }}

## Quick Start

1. Read `.index/AGENT-INDEX.md` for instant context
2. Check `.index/merkle-tree.json` root hash for change detection
3. Run `tests/run-tests.sh` before any changes

## Package Structure

```
{{ team_slug }}/
├── .index/           # Merkle tree (O(1) change detection)
├── agents/           # {{ team_name }} team agents
├── entities/         # Work items (Claude Code aligned)
├── lib/              # Shared code
├── docs/             # Documentation
├── tests/            # Integration tests
└── CLAUDE.md         # This file
```

## Team Agents

| Agent | Model | Turns | Role |
|-------|-------|-------|------|
| `{{ team_slug }}-lead` | {{ lead_model }} | {{ lead_turns }} | Team leadership, planning |
| `{{ team_slug }}-engineer` | {{ engineer_model }} | {{ engineer_turns }} | Implementation |
| `{{ team_slug }}-tester` | {{ tester_model }} | {{ tester_turns }} | Testing, validation |

## Steering Integration

This team uses budget-aware steering. See `../steering/` for configuration.

**Wrap-up Threshold**: {{ (wrap_up_threshold * 100)|int }}%

When budget ratio reaches {{ (wrap_up_threshold * 100)|int }}%:
1. Stop new work
2. Synthesize findings
3. Generate `<handoff>` YAML block
4. Document incomplete items

## Entry Checklist

- [ ] Read this file completely
- [ ] Run `python3 .index/check-changes.py` to verify index freshness
- [ ] Run `./tests/run-tests.sh` to confirm system health
- [ ] Check `entities/examples/` for current states

## Patterns to Follow

### Test-Driven Development
- [ ] **Red**: Write failing test first
- [ ] **Green**: Minimal code to pass
- [ ] **Refactor**: Clean up, maintain tests green

### Fail Fast Philosophy
- [ ] **Crash early**: Throw on invalid state
- [ ] **Validate inputs**: Check at boundaries
- [ ] **Explicit errors**: Descriptive messages

## Contributing

### Conventional Commits
```
<type>(<scope>): <description>

feat({{ team_slug }}): add new capability
fix({{ team_slug }}): correct issue
test({{ team_slug }}): add integration tests
```

### PR Checklist
- [ ] All tests pass
- [ ] Index regenerated
- [ ] Frontmatter complete
- [ ] Version bumped
- [ ] Conventional commit message
